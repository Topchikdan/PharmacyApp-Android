package com.example.exam.ui.theme

/**  Stub: приложение построено на XML, поэтому настоящая тема Material3
 *   не требуется.  Функция оставлена, чтобы старые вызовы ExamTheme {…}
 *   компилировались, но она просто исполняет переданный блок. */
inline fun ExamTheme(
    darkTheme: Boolean = false,
    dynamicColor: Boolean = false,
    content: () -> Unit
) {
    content()       // ничего больше не делаем
}
